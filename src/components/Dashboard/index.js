import React, { Component } from 'react';
import Menu from '../Menu';

class Dashboard extends Component {
  render() {
    return (
      <section>
        <Menu />
        dashboard, i am logged in
      </section>
    );
  }
}

export default Dashboard;
